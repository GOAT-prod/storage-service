{
    "port": 80,
    "databases" : {
        "need_mocks" : false,
        "mongo": {
            "connection" : "mongodb://localhost:27017",
            "database": "logs",
            "collection" : "storage-service"
        },
        "postgres": "postgres://postgres:postgres@localhost/storage?search_path=public&sslmode=disable"
    }
}